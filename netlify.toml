# See https://docs.netlify.com/configure-builds/file-based-configuration/#sample-file
# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]

  # Directory (relative to root of your repo) that contains the deploy-ready
  # HTML files and assets generated by the build. If a base directory has
  # been specified, include it in the publish directory path.
  publish = "_site"

  command = "npm run build"

[[redirects]]
  from = "https://citylab-hackthecrisis.netlify.com/*"
  to = "https://hackthecrisis.citylab-berlin.org/:splat"
  status = 301
  force = true

[[redirects]]
  from = "http://citylab-hackthecrisis.netlify.com/*"
  to = "http://hackthecrisis.citylab-berlin.org/:splat"
  status = 301
  force = true

[[redirects]]
  from = "http://hackthecrisis.citylab-berlin.org/*"
  to = "https://hackthecrisis.citylab-berlin.org/:splat"
  status = 301
  force = true

# The following redirect is intended for use with most SPAs that handle
# routing internally.
# will be needed when we are running a spa
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200